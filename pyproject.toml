[project]
name = "demo-streamlit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Calvin Volkmann", email = "calvin.volkmann@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "pandas-stubs~=3.0.5",
    "plotly>=7.1.0",
    "streamlit>=1.64.0",
    "yfinance>=1.7.0",
]

[project.scripts]
demo-streamlit = "demo_streamlit:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
